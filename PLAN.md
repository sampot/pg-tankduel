# 戰車對決（`pg-tankduel`）— 遊戲規劃文檔

> **用途：** 本 repo 的遊戲權威規格——coding agent 改動前必讀：這個遊戲是什麼、規則、設計限制、優化方向。
> **整理方式：** 從本 repo 實作反向整理（2026-08-23）。**改玩法先改此檔再改碼**；本檔與程式碼衝突時，以「規則（§3）」描述的設計意圖為準回報差異。
> **上游契約：** [PG-GAME-AGENT-GUIDE.md](https://github.com/sampot/playgrounds/blob/main/docs/PG-GAME-AGENT-GUIDE.md)（唯一必讀；本檔不重複其全文）· 型錄條目 `playgrounds/catalog/entries/pg-tankduel.yaml`

## 1. 一句話

虛擬搖桿開坦克、轉炮塔繞掩體互轟的俯視街機對決——五發命中擊毀對手即勝，可人機或雙 AI 觀戰；致敬俯視坦克類型玩法，非任一商業作品復刻。

## 2. 定案速覽

| 項 | 值 |
| --- | --- |
| catalog id / kind / series | `pg-tankduel` / `game` / `街機` |
| status | `listed` |
| 模式 | 人 vs AI（`pve`）／AI vs AI 觀戰（`aivai`）；單局制 |
| 勝負 | 敵方 HP 歸零勝、我方歸零敗；雙方各 **5** HP（`MAX_HP=5`），單發扣 1 |
| 視角/畫布 | 直向俯視 360×520 邏輯畫布；手機直持優先 |
| 對手 AI | 兩種風格（style 0 猛攻／style 1 拉距側移）；含隨機性，非決定性 |
| 素材 | 全部程式繪製（`sprites.js`）＋WebAudio 合成音效（無圖檔資產） |
| 持久化 | **無**——分數/勝場僅記憶體（見 §5） |
| 交付形 | 純 HTML＋CSS＋ESM JS；無 build；`npx vitest run` 測試 |

## 3. 完整規則（現行實作）

### 3.1 目標與勝負

- 玩家操綠方（下場）、AI 操紅方（上場）；炮彈命中扣 1 HP，HP 到 0 判負。
- 命中未擊毀時 pve 加 20 分；擊毀敵方 pve 加 100 分並記一次 `wins++`（僅記憶體）；aivai 不計分。無回合數、無連勝保存。
- 開局佈置：玩家 `(W/2, H−56)` 朝上、敵方 `(W/2, 56)` 朝下；敵方冷卻初始 0.35s（比玩家晚開火）、玩家 0。

### 3.2 場地與物理參數（實際值）

| 參數 | 值 | 意義 |
| --- | --- | --- |
| 畫布 | W=360, H=520 | 邏輯座標；邊界內縮 r+4 |
| 戰車半徑/速度 | TANK_R=16 / TANK_SPEED=95 px/s | 車體碰撞半徑 32 才互斥 |
| 轉向速度 | TURN_SPEED=3.2 rad/s | 車體旋轉上限 |
| 炮塔追蹤 | AI 上限 2.8 rad/s；玩家瞬時對準 | 玩家炮塔直接指向瞄準向量 |
| 炮彈 | BULLET_R=3.5、BULLET_SPEED=320、壽命 2.2s、砲口距 26px | 撞牆即爆不反彈 |
| 開火冷卻 | FIRE_COOLDOWN=0.55s | 雙方一致 |
| 掩體 | 5 座固定矩形（上下橫牆 140×18、左右直牆 18×100、中央方塊 60×60） | `makeWalls()` 固定佈局 |

### 3.3 操作與輸入合併（`input-controls.js`）

- 觸控：左下區「任意按住為圓心」拖曳出動態搖桿；位移經 `normalizeStickDrag` 正規化——死區 0.15、滿行程 remap 至單位圓（超過半徑鉗 1）。
- 鍵盤：WASD／方向鍵合成移動向量（斜向正規化）、空白鍵開火。
- 合併規則 `mergeInputSources`：搖桿活著時**搖桿恆權威**（即使回中也不讓位給鍵給鍵盤）；開火＝鍵盤 OR 觸控，任一來源按住即持續開火。
- 移動：輸入幅值 >0.15 才生效；車體先朝目標方向以轉向上限逼近、再以 `TANK_SPEED×min(1,mag)` 前進。幅值 >0.2 時炮塔指向瞄準向量，否則貼齊車體朝向。
- 放開、觸控取消、視窗失焦或頁面隱藏時所有觸控輸入歸零（`app.js`）。

### 3.4 AI 行為

- 兩種風格：style 0 猛攻（轉向係數 0.95、開火角窗 ±0.20 rad、偏好距離 110、車速 ×0.8）、style 1 拉距側移（0.78／±0.14／170／×0.68）。pve 的玩家車固定 style 0（不用），敵車固定 style 1；aivai 綠 style 0、紅 style 1。
- 炮塔每步朝目標收斂（≤2.8 rad/s）；滿足「瞄準角差 < aimSnap 且距離 <300 且冷卻完畢且視線通暢」才開火。視線判定＝12 點取樣線段、採樣半徑 2 的 `hasLos`。
- 距離策略：近於偏好距 −40 倒退（×−0.6）、偏好距內減速 ×0.25、遠於偏好距 +80 加速 ×1.05。
- 側移節奏：`aiThink` 計時到 0 後隨機側移一步（style 1 間隔 0.35–1.25s、幅度 22px；style 0 為 0.35–0.90s、14px），方向隨機左右。
- **AI 用 `Math.random()`，非決定性**——測試不能逐幀重放 AI 行為（與 pg-airhockey 不同），只能測結構性質。

### 3.5 邊界處理

- 移動碰撞採軸分離（先 x 後 y）：越界（r+4 內縮）、撞牆（圓 vs AABB 最近點）、與存活友車重疊（<32px）都會擋該軸，可沿牆滑行、不會卡死。
- 炮彈出界或撞牆立即引爆（小爆炸＋`impact` 音效事件），不反彈、不穿牆；命中判定圓半徑 = TANK_R+BULLET_R。
- 擊毀後該車 `alive=false` 停止一切行動，爆炸特效與震屏照常衰減；狀態機 `ready→playing→won/lost`，重開只走 `start()`。

## 4. 操作與畫面

| 輸入 | 動作 |
| --- | --- |
| 左下動態搖桿（任意按住拖曳） | 移動＋車體轉向（僅 pve） |
| 右下「炮」鈕 | 按住持續開火（可與搖桿同時操作） |
| WASD／方向鍵＋空白鍵 | 桌面等效操作 |
| 模式鈕 ×2 | 人 vs AI／AI vs AI 切換 |
| 開戰 | 開始或重開（非破壞性，不需確認） |
| 音效鈕 | 開/關（僅記憶體，未持久化） |

- HUD：分數、綠/紅雙方 HP、訊息列（開戰提示/勝敗文案）。
- 特效：爆炸（大 0.7s／小 0.35s）、震屏（大 10px／小 5px，衰減 28/s）、引擎低頻嗡聲隨油門變調（aivai 定值油門 0.35）。
- Mobile-first：直向單欄、canvas 上疊觸控層；禁 `alert`／`confirm`／`prompt`。

## 5. 持久化（KV 權威）

| key | 內容 | 讀寫時機 |
| --- | --- | --- |
| （無 KV） | — | 本 repo 未呼叫 `/api/kv`，也未用 localStorage |
| （無 localStorage） | 分數、wins、音效開關皆存於 JS 物件 | 重新載入即失 |

- 如實記錄：目前沒有任何跨局持久化。宿主 KV 無 per-SAM 命名空間，若新增持久化**必須自帶前綴**（建議 `pg-tankduel-*`，如 `pg-tankduel-wins`），禁止裸 key 直接寫 `/api/kv/{key}`。
- `functions.js` 僅為 Playgrounds Infrastructure stub（回 JSON echo），未提供任何資料 API。

## 6. 美術／音效／署名

- 無外部圖像資產、無 `assets/`、無 `ATTRIBUTION.md`：戰車（履帶/炮塔/散熱柵細節）、掩體、爆炸全部 canvas 程序繪製（`sprites.js`）。
- 音效全部 WebAudio 合成（`audio.js`，master 0.26）：炮擊、命中/爆炸（noise+filter）、引擎循環嗡聲；AudioContext 需手勢 unlock。無第三方取樣。
- 若未來加入素材：拷進 `assets/`、新建 `ATTRIBUTION.md`（CC0 也須署名）、同步 `sam-manifest.json` files 清單。

## 7. 測試（`npx vitest run`）

現有覆蓋（`input-controls.test.js`，6 例）：搖桿死區內歸零、死區外 remap 到單位圓（含對角）、radius 無效回零、鍵盤斜向正規化、搖桿活著時恆權威、開火 OR 語意（多來源任一按住即真）。

**遊戲邏輯（`game.js`）零測試**。最小必測建議：命中扣血至 0 判勝/敗、開火冷卻 0.55s 生效、炮彈撞牆即消失不穿牆、軸分離移動不嵌入掩體、車車互斥半徑 32、AI 開火需 LOS（牆後不開火）。

## 8. 硬約束（不可違反）

1. 僅 HTML＋CSS＋JS（ESM）；**無 build**、不入庫 `node_modules`、不安套件；工具一律 `npx <pkg>` 臨時執行。
2. 禁瀏覽器原生 `alert`／`confirm`／`prompt`；確認一律頁內 UI。
3. Mobile-first：虛擬搖桿＋開火鈕主操作在手機可用，不可 hover-only。
4. 分數/進度以 `fetch('/api/kv/…')` 為權威（現行尚無持久化；新增必須帶 `pg-tankduel-` 前綴）；禁止裸 localStorage 當權威。
5. 不自行載入 `sdk.js`；宿主注入 `window.PG`。本作未用 `PG.libs`（vanilla 即可，勿無故引入 Phaser）。
6. 改動可執行邏輯前先寫失敗測試（TDD）；`game.js` 目前缺測是債不是常態。
7. 檔案清單變動須同步 `sam-manifest.json`（下載契約）。

## 9. 優化建議（可玩性與樂趣）

依優先級；實作前先在此登記並補測試。原則：強化手感與重玩誘因，不改變「俯視坦克互轟」的核心認同。

**高優先**

1. **勝場持久化**：`wins` 目前重新載入即失。以 `/api/kv/pg-tankduel-wins` 存生涯勝場＋最高連勝，HUD 顯示——給單局制一個跨局目標，成本極低。
2. **AI 難度三段**：現行只有紅方拉距一種打法。把 `speedMul`/`aimSnap`/`aiThink` 包成 easy/normal/hard 參數組（如 hard 收窄 aiThink 間隔、放寬開火角窗），解決熟練後單調。
3. **掩體破壞**：固定 5 座掩體讓走位套路很快被摸透；給掩體 HP（受擊 3 發碎裂、碎塊粒子），每局地形會演化，大幅提升對局變化。
4. **遊戲核心補測**：§7 所列 game.js 邊界測試先行落地，否則上述改動都在裸奔。

**中優先**

5. **道具掉落**：擊中掩體或定時空投修理包/速射（冷卻減半 10s），創造離開掩體的誘因，打破龜縮僵局。
6. **連續挑戰制**：勝利後敵方 HP+1 或 AI 參數增強進入下一輪，記錄最佳輪次到 KV——把「打贏一台」升級成「撐到第幾台」。
7. **手感細化**：開炮後座（車體微退＋炮管縮回動畫）、履帶揚塵粒子、命中率統計顯示；不改規則只加回饋。

**低優先**

8. **地圖種子**：`makeWalls()` 改為可從種子生成多套對稱佈局（現行固定佈局寫死在函式內）；配合第 6 條輪次換圖。
9. **音量設定**：master 0.26 寫死；提供音量滑桿並以 `pg-tankduel-sfx` localStorage 快取＋KV 回寫（沿用 airhockey 模式）。
